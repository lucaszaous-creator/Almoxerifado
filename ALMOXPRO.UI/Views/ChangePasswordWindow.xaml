<Window x:Class="ALMOXPRO.UI.Views.ChangePasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Alterar Senha"
        Height="400" Width="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{materialDesign:MaterialDesignFont}">
    <StackPanel Margin="24" VerticalAlignment="Center">
        <TextBlock Text="Alterar senha" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,16" />
        <PasswordBox x:Name="CurrentPassword"
                     materialDesign:HintAssist.Hint="Senha atual"
                     Style="{StaticResource MaterialDesignOutlinedPasswordBox}" Margin="0,0,0,12" />
        <PasswordBox x:Name="NewPassword"
                     materialDesign:HintAssist.Hint="Nova senha"
                     Style="{StaticResource MaterialDesignOutlinedPasswordBox}" Margin="0,0,0,12" />
        <PasswordBox x:Name="ConfirmPassword"
                     materialDesign:HintAssist.Hint="Confirmar nova senha"
                     Style="{StaticResource MaterialDesignOutlinedPasswordBox}" Margin="0,0,0,8" />
        <TextBlock Text="{Binding ErrorMessage}" Foreground="#D32F2F" TextWrapping="Wrap"
                   MinHeight="20" Margin="0,4" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="CANCELAR" Style="{StaticResource MaterialDesignFlatButton}"
                    Click="OnCancelClick" Margin="0,0,8,0" />
            <Button Content="CONFIRMAR" Click="OnConfirmClick" />
        </StackPanel>
    </StackPanel>
</Window>
